# 推荐算法模块详细解析

## 目录

1. [模块概览](#1-模块概览)
2. [新增文件清单](#2-新增文件清单)
3. [接口文档](#3-接口文档)
4. [SimilarityUtil — 相似度计算工具](#4-similarityutil--相似度计算工具)
5. [RecommendServiceImpl — 核心算法实现](#5-recommendserviceimpl--核心算法实现)
   - 5.1 [相似案例推荐](#51-相似案例推荐)
   - 5.2 [个性化推荐](#52-个性化推荐)
   - 5.3 [热门推荐](#53-热门推荐)
   - 5.4 [UserProfile 用户画像内部类](#54-userprofile-用户画像内部类)
   - 5.5 [四层降级策略（v2 优化）](#55-四层降级策略v2-优化)
6. [RecommendController — 接口层](#6-recommendcontroller--接口层)
7. [VO 设计说明](#7-vo-设计说明)
8. [完整数据流](#8-完整数据流)
9. [边界情况与异常处理](#9-边界情况与异常处理)
10. [算法公式汇总](#10-算法公式汇总)
11. [后续扩展方向](#11-后续扩展方向)
12. [v2 优化记录](#12-v2-优化记录)

---

## 1. 模块概览

本模块基于项目中已有的 `BrowseHistory`（浏览记录）、`UserFavorite`（收藏记录）、`LegalCase`（案例，含 AI 提取的 `keywords`/`caseReason`/`importanceScore` 等字段），实现了**三种推荐策略**：

| 策略 | 接口 | 算法核心 | 适用场景 |
|------|------|----------|----------|
| 相似案例推荐 | `GET /cases/{id}/similar` | Jaccard 相似度 + 加权打分 | 案例详情页"相关案例" |
| 个性化推荐 | `GET /recommend/personalized` | 用户兴趣画像 + 质量因子 | 首页个性化推荐流 |
| 热门推荐 | `GET /recommend/popular` | 评分 + 浏览 + 收藏 归一化加权 | 首页榜单、未登录默认推荐 |

**零侵入设计**：未修改任何现有文件，全部为新增代码。

---

## 2. 新增文件清单

```
src/main/java/org/example/demo1/
├── util/
│   └── SimilarityUtil.java              ← 相似度计算工具
├── vo/
│   ├── RecommendCaseVO.java             ← 推荐结果 VO（含相似度字段）
│   └── PersonalizedRecommendVO.java     ← 个性化推荐结果 VO
├── service/
│   ├── RecommendService.java            ← 服务接口
│   └── impl/
│       └── RecommendServiceImpl.java    ← 核心算法实现（349行）
└── controller/
    └── RecommendController.java         ← REST 接口（3个）
```

---

## 3. 接口文档

### 3.1 相似案例推荐

```
GET /cases/{id}/similar?limit=6
```

**权限**：公开，无需登录

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| id | Long | 目标案例 ID |

**查询参数**：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| limit | Integer | 6 | 返回数量，范围 [1, 20] |

**响应示例**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 102,
      "titleZh": "某某知识产权侵权纠纷案",
      "country": "美国",
      "caseType": 4,
      "importanceScore": 85,
      "keywords": "知识产权,专利,侵权",
      "similarityScore": 0.72,
      "similarityReason": "关键词高度重合·案由相似·同国家同类型"
    }
  ]
}
```

---

### 3.2 个性化推荐

```
GET /recommend/personalized?limit=10
```

**权限**：公开；已登录用户返回个性化结果，未登录自动降级为热门推荐

**查询参数**：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| limit | Integer | 10 | 返回数量，范围 [1, 20] |

**响应示例（已登录，有行为数据）**：

```json
{
  "code": 200,
  "data": {
    "basis": "personalized",
    "preferTags": ["知识产权", "美国", "商标", "专利", "商事"],
    "cases": [ /* CaseListVO 列表 */ ]
  }
}
```

**响应示例（未登录或无行为数据）**：

```json
{
  "code": 200,
  "data": {
    "basis": "popular",
    "preferTags": null,
    "cases": [ /* 热门案例列表 */ ]
  }
}
```

> **前端使用建议**：根据 `basis` 字段决定展示文案。`"personalized"` 时展示"根据您的兴趣推荐"并展示 `preferTags`；`"popular"` 时展示"热门案例"。

---

### 3.3 热门推荐

```
GET /recommend/popular?limit=10
```

**权限**：公开，无需登录

**查询参数**：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| limit | Integer | 10 | 返回数量，范围 [1, 20] |

**响应示例**：

```json
{
  "code": 200,
  "data": [
    {
      "id": 55,
      "titleZh": "某某重大商事仲裁案",
      "importanceScore": 96,
      "viewCount": 1280,
      "favoriteCount": 87,
      "similarityScore": 0.89,
      "similarityReason": "综合热门推荐"
    }
  ]
}
```

---

## 4. SimilarityUtil — 相似度计算工具

路径：`util/SimilarityUtil.java`

### 4.1 Jaccard 相似度（关键词匹配）

```java
public static double jaccardSimilarity(String keywordsA, String keywordsB)
```

**算法原理**：

Jaccard 相似系数是信息检索中处理集合相似度的经典算法：

```
Jaccard(A, B) = |A ∩ B| / |A ∪ B|
```

**计算示例**：

```
案例A keywords = "知识产权,专利,侵权"   → Set A = {知识产权, 专利, 侵权}
案例B keywords = "知识产权,商标,侵权"   → Set B = {知识产权, 商标, 侵权}

交集 = {知识产权, 侵权}  → |交集| = 2
并集 = {知识产权, 专利, 侵权, 商标}  → |并集| = 4

Jaccard = 2 / 4 = 0.5
```

**选用 Jaccard 的原因**：
- 天然适合无序关键词集合，不受顺序影响
- 自动处理集合大小差异（分母是并集）
- 计算结果在 [0, 1] 之间，无需归一化
- 实现简单，无外部依赖

---

### 4.2 案由相似度（字符串包含匹配）

```java
public static double caseReasonSimilarity(String reasonA, String reasonB)
```

**返回值规则**：

| 情况 | 返回值 | 示例 |
|------|--------|------|
| 完全相同 | 1.0 | `"知识产权侵权纠纷"` vs `"知识产权侵权纠纷"` |
| 互相包含（上下位案由） | 0.6 | `"知识产权纠纷"` vs `"知识产权侵权纠纷"` |
| 无关 | 0.0 | `"合同纠纷"` vs `"知识产权侵权纠纷"` |

**为什么用包含关系而不是编辑距离**：

中文法律案由存在明确的层级关系（上位案由 → 下位案由），包含关系能精准捕获这种法律分类层级，而编辑距离或余弦相似度对此类场景反而过于复杂且效果不佳。

---

### 4.3 国家 + 案件类型匹配度

```java
public static double typeAndCountrySimilarity(Integer typeA, String countryA,
                                               Integer typeB, String countryB)
```

**返回值规则**：

| 匹配情况 | 返回值 | 原因 |
|----------|--------|------|
| 国家 + 类型均匹配 | 1.0 | 最强相关性 |
| 仅类型匹配 | 0.5 | 案件类型决定适用法律体系，相关性较强 |
| 仅国家匹配 | 0.3 | 同国司法实践有参考价值，但弱于类型 |
| 均不匹配 | 0.0 | 无分类相关性 |

**类型权重 > 国家权重的原因**：对于法律研究者，`案件类型`（民事/刑事/行政/商事）决定了适用的实体法规则，是更核心的检索维度。

---

### 4.4 综合相似度（加权求和）

```java
public static double compositeSimilarity(...)
```

```
综合相似度 = Jaccard相似度 × 0.5
           + 案由相似度   × 0.3
           + 国家/类型    × 0.2
```

**权重分配依据**：
- 关键词（50%）：AI 提取的关键词是最精准的主题标识，权重最高
- 案由（30%）：法律分类的直接标签，高度相关
- 国家/类型（20%）：背景约束条件，辅助过滤

---

## 5. RecommendServiceImpl — 核心算法实现

路径：`service/impl/RecommendServiceImpl.java`

### 5.1 相似案例推荐

#### 完整执行流程

```
similarCases(caseId, limit)
    │
    ├─① selectById(caseId) → 获取目标案例
    │
    ├─② fetchCandidates(caseType, country, excludeId)
    │       SQL: WHERE ai_status=2 AND id != caseId
    │            AND (case_type = ? OR country = ?)
    │            LIMIT 200
    │       → 两阶段筛选：先用索引字段预过滤，缩小候选集
    │
    ├─③ Stream.map(scoreSimilarity)
    │       → 对每个候选计算三维相似度
    │       → 综合分 = kwSim×0.5 + reasonSim×0.3 + typeSim×0.2
    │
    ├─④ filter(similarityScore >= 0.15)
    │       → 过滤掉无实质相关的案例
    │
    ├─⑤ sorted(desc by similarityScore)
    │
    └─⑥ limit(safeLimit) → 返回前 N 条
```

#### 两阶段筛选设计

```java
// 第一阶段：SQL 预过滤（利用数据库索引）
LambdaQueryWrapper<LegalCase> wrapper = new LambdaQueryWrapper<LegalCase>()
        .eq(LegalCase::getAiStatus, 2)
        .ne(LegalCase::getId, excludeId)
        .and(w -> w
                .eq(caseType != null, LegalCase::getCaseType, caseType)
                .or()
                .eq(country != null && !country.isBlank(), LegalCase::getCountry, country)
        )
        .last("LIMIT " + CANDIDATE_POOL_SIZE); // CANDIDATE_POOL_SIZE = 200
```

> ⚠️ 注意 `.and(w -> w.eq(...).or().eq(...))` 的括号写法。
> 如果去掉 `.and(...)` 直接写 `.eq(...).or().eq(...)`，
> MyBatis-Plus 会生成优先级错误的 SQL：
> ```sql
> -- 错误（OR 优先级与 AND 混淆）
> WHERE ai_status=2 AND id!=? AND case_type=? OR country=?
>
> -- 正确（OR 被括号包裹）
> WHERE ai_status=2 AND id!=? AND (case_type=? OR country=?)
> ```

```java
// 第二阶段：内存精排（Java Stream）
return candidates.stream()
        .map(c -> scoreSimilarity(target, c))
        .filter(vo -> vo.getSimilarityScore() >= SIMILARITY_THRESHOLD)
        .sorted(Comparator.comparingDouble(RecommendCaseVO::getSimilarityScore).reversed())
        .limit(safeLimit)
        .collect(Collectors.toList());
```

#### 相似理由生成

```java
private String buildSimilarityReason(double kwSim, double reasonSim, double typeSim) {
    List<String> reasons = new ArrayList<>();
    if (kwSim >= 0.3)      reasons.add("关键词高度重合");
    else if (kwSim > 0)    reasons.add("关键词部分重合");
    if (reasonSim >= 0.6)  reasons.add("案由相似");
    if (typeSim >= 1.0)    reasons.add("同国家同类型");
    else if (typeSim >= 0.5) reasons.add("同案件类型");
    else if (typeSim >= 0.3) reasons.add("同国家");
    return reasons.isEmpty() ? "内容相关" : String.join("·", reasons);
}
```

各阈值对应的语义：

| 维度 | 阈值 | 语义 |
|------|------|------|
| kwSim | ≥ 0.3 | 并集中约30%以上词汇重合 |
| kwSim | > 0 | 至少有1个关键词重合 |
| reasonSim | ≥ 0.6 | contains 关系（上下位案由） |
| typeSim | 1.0 | 国家和类型都匹配 |
| typeSim | 0.5 | 仅案件类型匹配 |
| typeSim | 0.3 | 仅国家匹配 |

---

### 5.2 个性化推荐

#### 完整执行流程（v2 四层降级）

```
personalizedRecommend(userId, limit)
    │
    ├─① userId == null?
    │       是 → 第一层降级：未登录（basis="popular"）
    │
    ├─② buildUserProfile(userId)
    │       ├─ userFavoriteMapper.selectList → 全部收藏（权重3）
    │       ├─ browseHistoryMapper.selectList → 近30条浏览（权重1）
    │       └─ 只对 aiStatus=2 的案例提取 caseType/country/keywords 特征
    │              → Map<caseType, weight>
    │              → Map<country, weight>
    │              → Map<keyword, weight>
    │
    ├─③ profile.getInteractedCaseIds().isEmpty() == true?
    │       是 → 第二层降级：真正冷启动，无任何行为（basis="new_user"）
    │
    ├─④ profile.isEmpty() == true?  （有行为但特征字段全为空）
    │       是 → 第三层降级：字段缺失，无法建模（basis="new_user"）
    │
    ├─⑤ fetchPersonalizedCandidates(excludeIds)
    │       SQL: WHERE ai_status=2 AND id NOT IN (已交互ID集合) LIMIT 200
    │
    ├─⑥ 对每个候选计算 calcPersonalizedScore
    │       score = interestScore × log(1 + importanceScore/10)
    │
    ├─⑦ filter(score > 0) → 过滤完全不匹配
    │
    ├─⑧ ranked.isEmpty()?
    │       是 → 第四层降级：候选集无匹配（basis="popular"）
    │
    └─⑨ 返回 PersonalizedRecommendVO
            basis = "personalized"
            preferTags = profile.getTopTags(5)
            cases = ranked
```

#### 用户兴趣画像构建

```java
// 收藏（显式反馈）权重 = 3，浏览（隐式反馈）权重 = 1
double weight = favoriteCaseIds.contains(lc.getId()) ? 3.0 : 1.0;

// 特征累加
profile.getCaseTypeWeights().merge(lc.getCaseType(), weight, Double::sum);
profile.getCountryWeights().merge(lc.getCountry(), weight, Double::sum);
for (String kw : SimilarityUtil.parseKeywords(lc.getKeywords())) {
    profile.getKeywordWeights().merge(kw, weight, Double::sum);
}
```

**收藏权重是浏览权重 3 倍的依据**：

| 行为类型 | 信号类型 | 意图明确性 |
|----------|----------|-----------|
| 浏览（Implicit Feedback） | 隐式 | 低（可能误点、快速扫过） |
| 收藏（Explicit Feedback） | 显式 | 高（主动操作，表达持续兴趣） |

3:1 是推荐系统中隐式/显式反馈的常用经验比例（Netflix、YouTube 等系统均有类似处理）。

#### 个性化评分公式

```
推荐分 = 兴趣匹配分 × log(1 + importanceScore / 10)

兴趣匹配分 = caseTypeWeights[caseType]
           + countryWeights[country]
           + Σ keywordWeights[kw]  (对案例每个关键词累加)
```

**`Math.log1p` 对数平滑的作用**：

若直接线性乘法 `interestScore × importanceScore`，评分100分案例比50分案例得分高出2倍，导致高分案例垄断推荐，多样性差。

使用对数压缩后：

```
log(1 + 100/10) = log(11) ≈ 2.40
log(1 + 50/10)  = log(6)  ≈ 1.79
差距：2.40 / 1.79 ≈ 1.34 倍（vs 线性的 2 倍）
```

压缩差距后，中等评分的高兴趣匹配案例也有机会出现在推荐结果中，提升推荐多样性。

---

### 5.3 热门推荐

#### 热门分计算公式

```
热门分 = importanceScore/100 × 0.5
       + log(viewCount+1) / log(1001) × 0.3
       + log(favoriteCount+1) / log(101) × 0.2
```

```java
private double calcPopularScore(LegalCase lc) {
    double importance = (lc.getImportanceScore() != null ? lc.getImportanceScore() : 0) / 100.0;
    int vc = lc.getViewCount() != null ? lc.getViewCount() : 0;
    int fc = lc.getFavoriteCount() != null ? lc.getFavoriteCount() : 0;
    double viewNorm  = Math.log1p(vc) / Math.log1p(1000);  // 假设最大浏览量约1000
    double favNorm   = Math.log1p(fc) / Math.log1p(100);   // 假设最大收藏量约100
    return importance * 0.5 + viewNorm * 0.3 + favNorm * 0.2;
}
```

**各维度归一化到 [0,1] 的原因**：三个指标量纲不同（0-100分 / 几千次浏览 / 几百次收藏），不归一化直接加权会导致数值大的维度完全主导结果。

**权重分配依据**：

| 维度 | 权重 | 含义 |
|------|------|------|
| importanceScore | 50% | AI 评估的客观内容质量，最可靠 |
| viewCount | 30% | 规模信号，但含噪（路过点击） |
| favoriteCount | 20% | 高质量信号，但数量少 |

---

### 5.4 UserProfile 用户画像内部类

```java
private static class UserProfile {
    private final Set<Long> interactedCaseIds = new HashSet<>();
    private final Map<Integer, Double> caseTypeWeights = new HashMap<>();
    private final Map<String, Double> countryWeights = new HashMap<>();
    private final Map<String, Double> keywordWeights = new HashMap<>();

    public boolean isEmpty() { ... }
    public List<String> getTopTags(int topN) { ... }
}
```

**为什么是 `private static class` 而非独立文件**：
- `UserProfile` 仅在推荐计算期间作为中间态临时使用，无需外部引用
- 声明为 `static` 是因为不需要访问外部类实例，避免隐式持有外部类引用（内存安全）
- 放在内部类中保持代码内聚，避免污染 `vo/` 包

**`buildUserProfile` 只查 `aiStatus=2` 的案例（v2 修复）**：

原始版本使用 `selectBatchIds(allIds)` 不过滤状态，用户浏览过的未完成案例（`caseType/country/keywords` 为 NULL）会被纳入特征提取，造成三个 Map 全为空、画像误判为"无数据"。v2 改为：

```java
List<LegalCase> interactedCases = legalCaseMapper.selectList(
    new LambdaQueryWrapper<LegalCase>()
        .in(LegalCase::getId, allIds)
        .eq(LegalCase::getAiStatus, 2));  // 只用 AI 处理完成的案例建立画像
```

**`getTopTags` 合并 country + keyword，不含 caseType**：

`caseType` 是整数枚举（1=民事，4=商事），直接展示给用户没有可读语义。`country` 和 `keyword` 是有意义的字符串标签，可直接用于前端展示"根据您的兴趣：知识产权 / 美国 / 商事纠纷"。

```java
public List<String> getTopTags(int topN) {
    Map<String, Double> merged = new HashMap<>();
    countryWeights.forEach((k, v) -> merged.merge(k, v, Double::sum));
    keywordWeights.forEach((k, v) -> merged.merge(k, v, Double::sum));
    return merged.entrySet().stream()
            .sorted(Map.Entry.<String, Double>comparingByValue().reversed())
            .limit(topN)
            .map(Map.Entry::getKey)
            .collect(Collectors.toList());
}
```

---

## 6. RecommendController — 接口层

路径：`controller/RecommendController.java`

### 路由设计说明

- `GET /cases/{id}/similar`：挂在案例资源路径下，符合 RESTful 语义（"案例的相关案例"是案例资源的子资源）
- `GET /recommend/*`：独立的推荐功能路径，与 CRUD 路由隔离

### limit 参数双重防御

```java
@RequestParam(defaultValue = "6") Integer limit   // ①处理缺省值（Spring 自动转换）
int n = limit == null ? 6 : Math.min(Math.max(limit, 1), 20);  // ②防止越界值
```

### 个性化接口不需要 `@LoginRequired`

```java
@GetMapping("/recommend/personalized")
public Result<PersonalizedRecommendVO> personalizedRecommend(...) {
    Long userId = UserContext.getUserId();  // 未登录时返回 null
    // Service 层负责降级逻辑
    return Result.success(recommendService.personalizedRecommend(userId, n));
}
```

同一个接口同时服务登录和未登录用户，前端无需根据登录状态调用不同接口，通过响应中的 `basis` 字段区分实际推荐方式。

---

## 7. VO 设计说明

### RecommendCaseVO

在案例基础信息之上，新增两个推荐专属字段：

```java
private Double similarityScore;   // 与目标案例的综合相似度 [0.0, 1.0]
private String similarityReason;  // 如"关键词高度重合·案由相似·同国家同类型"
```

没有直接复用 `CaseListVO` 的原因：`CaseListVO` 含有 `isFavorited`、`isDeleted`、`fastgptSyncStatus` 等管理端字段，推荐场景无需这些字段；同时 `similarityScore` 只在推荐语境下有含义，放入通用 VO 会造成字段歧义。

### PersonalizedRecommendVO

```java
private String basis;              // "personalized" | "new_user" | "popular"
private List<String> preferTags;   // 用户偏好标签，仅 basis="personalized" 时有值
private List<CaseListVO> cases;    // 推荐案例列表
```

`basis` 字段是前端渲染决策的依据（v2 新增 `new_user`）：

```
basis = "personalized"
  → 标题："✨ 为您推荐"
  → 展示 preferTags 偏好标签气泡

basis = "new_user"
  → 标题："🔥 热门案例"
  → 展示提示条："💡 浏览或收藏案例后，将为您开启个性化推荐"
  → 含义：用户已登录，但暂无有效行为数据（冷启动）

basis = "popular"
  → 标题："🔥 热门案例"
  → 无额外提示
  → 含义：未登录，或候选集与偏好完全无交集
```

---

## 8. 完整数据流

### 相似案例推荐

```
GET /cases/101/similar?limit=6
        ↓
RecommendController.similarCases(101, 6)
        ↓
RecommendServiceImpl.similarCases(101, 6)
  [1] legalCaseMapper.selectById(101) → 目标案例 {caseType=4, country="美国", keywords="知识产权,专利"}
  [2] fetchCandidates(caseType=4, "美国", 101)
        SQL: WHERE ai_status=2 AND id!=101 AND (case_type=4 OR country='美国') LIMIT 200
        → candidates: 150条
  [3] Stream.map(scoreSimilarity)
        候选案例102: kwSim=0.5, reasonSim=1.0, typeSim=1.0
        composite = 0.5×0.5 + 1.0×0.3 + 1.0×0.2 = 0.75
  [4] filter(score >= 0.15) → 保留80条
  [5] sorted(desc) → [0.75, 0.63, 0.51, ...]
  [6] limit(6) → 取前6条
        ↓
返回 List<RecommendCaseVO> (6条，含similarityScore和similarityReason)
```

### 个性化推荐

```
GET /recommend/personalized?limit=10
        ↓
UserContext.getUserId() → userId=42
        ↓
RecommendServiceImpl.personalizedRecommend(42, 10)
  [1] buildUserProfile(42)
        收藏: [案例A(商事/美国/知识产权), 案例B(商事/美国/专利)] → 权重3
        浏览: [案例C(民事/日本), 案例A, 案例D(商事/美国/商标)] → 权重1
        画像:
          caseTypeWeights: {4→7.0, 1→1.0}  (商事=3+3+1=7, 民事=1)
          countryWeights:  {"美国"→7.0, "日本"→1.0}
          keywordWeights:  {"知识产权"→3.0, "专利"→3.0, "商标"→1.0}
          interactedIds:   {A, B, C, D}
  [2] fetchPersonalizedCandidates({A,B,C,D})
        SQL: WHERE ai_status=2 AND id NOT IN (A,B,C,D) LIMIT 200
        → candidates: 196条
  [3] calcPersonalizedScore(案例E)
        案例E: caseType=4, country="美国", keywords="知识产权,版权"
        interestScore = 7.0(类型) + 7.0(国家) + 3.0(知识产权) = 17.0
        qualityFactor = log(1 + 82/10) = log(9.2) ≈ 2.22
        score = 17.0 × 2.22 = 37.7
  [4] sorted + limit(10) → 取前10条
        ↓
返回 PersonalizedRecommendVO {
  basis: "personalized",
  preferTags: ["美国", "知识产权", "专利", "商标", "日本"],
  cases: [10条案例]
}
```

---

## 9. 边界情况与异常处理

| 情况 | 处理方式 | 响应 |
|------|----------|------|
| 目标案例 ID 不存在 | 抛 `BusinessException(CASE_NOT_EXIST)` | `{"code": 2001, "message": "案例不存在"}` |
| 目标案例无关键词字段 | `jaccardSimilarity` 返回 0.0，退化为仅靠案由+类型打分 | 正常返回（可能条数少） |
| 所有候选相似度 < 0.15 | 返回空列表 | `{"code": 200, "data": []}` |
| 用户未登录 | `userId = null`，降级热门推荐 | `basis: "popular"` |
| 用户从未浏览/收藏 | `profile.isEmpty() = true`，降级热门推荐 | `basis: "popular"` |
| 有画像但候选全部不匹配 | `ranked.isEmpty()`，降级热门推荐 | `basis: "popular"` |
| `importanceScore` 为 null | 热门推荐默认 0；个性化默认 50（中等质量） | 正常计算，不报错 |
| `viewCount`/`favoriteCount` 为 null | 默认 0，`log1p(0) = 0`，不影响计算 | 正常计算，不报错 |
| `limit` 传入 0 或负数 | `Math.max(limit, 1)` 修正为 1 | 正常返回 1 条 |
| `limit` 传入超过 20 | `Math.min(limit, 20)` 修正为 20 | 正常返回 20 条 |

---

## 10. 算法公式汇总

### 相似案例推荐

```
综合相似度 = Jaccard(keywordsA, keywordsB) × 0.5
           + caseReasonSim(reasonA, reasonB) × 0.3
           + typeCountrySim(typeA, countryA, typeB, countryB) × 0.2

Jaccard(A, B) = |A∩B| / |A∪B|

caseReasonSim = 1.0  (完全相同)
              | 0.6  (包含关系)
              | 0.0  (无关)

typeCountrySim = 1.0  (类型+国家均匹配)
               | 0.5  (仅类型匹配)
               | 0.3  (仅国家匹配)
               | 0.0  (均不匹配)
```

### 个性化推荐

```
推荐分 = interestScore × qualityFactor

interestScore = caseTypeWeights[caseType]
              + countryWeights[country]
              + Σ keywordWeights[kw]

qualityFactor = log(1 + importanceScore / 10)

画像权重: 收藏 = 3, 浏览 = 1
```

### 热门推荐

```
热门分 = (importanceScore / 100) × 0.5
       + (log(viewCount+1) / log(1001)) × 0.3
       + (log(favoriteCount+1) / log(101)) × 0.2
```

---

## 11. 后续扩展方向

### 扩展1：热门推荐结果缓存（5分钟）

热门推荐结果变化慢，可加 Spring Cache 避免重复计算：

```java
@Cacheable(value = "popularCases", key = "#limit")
@Override
public List<RecommendCaseVO> popularCases(int limit) { ... }
```

需在 `pom.xml` 引入 `spring-boot-starter-data-redis` 并配置 Redis。

### 扩展2：增加更多行为信号

当前仅区分浏览（权重1）和收藏（权重3），可进一步细化：

| 行为 | 建议权重 | 数据来源 |
|------|----------|----------|
| 浏览 | 1 | `BrowseHistory`（现有） |
| 收藏 | 3 | `UserFavorite`（现有） |
| 记笔记 | 5 | `CaseUserNote`（现有，未使用） |
| 搜索命中 | 2 | `SearchHistory`（现有，未使用） |

### 扩展3：相似度算法升级

当前使用基于关键词字符串的 Jaccard 相似度，可升级为：

1. **TF-IDF 向量余弦相似度**：考虑关键词在全库中的区分度（稀有关键词权重更高）
2. **FastGPT Embedding 语义相似度**：调用已集成的 FastGPT 获取文本语义向量，实现语义级别的相似搜索

**关键优势**：上述升级只需修改 `SimilarityUtil.java`，接口层和 Service 层均无需改动。

### 扩展4：A/B 测试支持

在 `PersonalizedRecommendVO` 的 `basis` 字段基础上，可扩展为策略标识（如 `"personalized_v1"` / `"personalized_v2"`），方便后端埋点对比不同推荐策略的点击率和转化率。

---

## 5.5 四层降级策略（v2 优化）

> 本节记录 v2 对个性化推荐降级逻辑的完整重设计。

### 背景：v1 存在的问题

v1 的降级逻辑只区分两种状态（`popular` / `personalized`），导致：

1. **已登录用户浏览了未完成 AI 处理的案例**（`aiStatus != 2`，`caseType/country/keywords` 为 NULL） → `buildUserProfile` 提取不到任何特征 → `profile.isEmpty() = true` → 被错误归类为"无数据"用户 → 始终返回 `popular`
2. **前端无法区分"未登录"与"已登录但暂无数据"**，无法给用户提供有意义的引导文案

### v2 四层降级设计

```
降级层级        触发条件                           basis 值    前端展示
────────────────────────────────────────────────────────────────────────
第一层（硬降级） userId == null（未登录）            popular     🔥 热门案例
第二层（冷启动） 无任何浏览/收藏记录                 new_user    🔥 热门案例 + 提示条
第三层（字段缺失）有记录但 AI 字段全为空              new_user    🔥 热门案例 + 提示条
第四层（无匹配） 候选集与偏好无交集（score 全为 0）   popular     🔥 热门案例
正常路径         有偏好且有匹配案例                  personalized ✨ 为您推荐 + 偏好标签
```

### 关键修复点

**修复1：`buildUserProfile` 改用 `aiStatus=2` 过滤**

```java
// v1（有缺陷）
List<LegalCase> interactedCases = legalCaseMapper.selectBatchIds(allIds);

// v2（修复后）
List<LegalCase> interactedCases = legalCaseMapper.selectList(
    new LambdaQueryWrapper<LegalCase>()
        .in(LegalCase::getId, allIds)
        .eq(LegalCase::getAiStatus, 2));  // 只用 AI 处理完成的案例
```

**修复2：`isEmpty()` 检查与行为记录检查分离**

```java
// v1：isEmpty() 判断特征是否为空（会误判"有行为但字段为空"为"无行为"）
if (profile.isEmpty()) → basis = "popular"

// v2：先判断有无行为，再判断特征是否可用
if (profile.getInteractedCaseIds().isEmpty()) → basis = "new_user"  // 真正无行为
if (profile.isEmpty())                        → basis = "new_user"  // 有行为但字段缺失
```

### 前端对应处理（`home.wxml`）

| `basis` | 标题 | 额外提示 | preferTags |
|---------|------|---------|-----------|
| `popular` | 🔥 热门案例 | 无 | 不展示 |
| `new_user` | 🔥 热门案例 | 💡 浏览或收藏案例后，将为您开启个性化推荐 | 不展示 |
| `personalized` | ✨ 为您推荐 | 无 | 展示 |

---

## 12. v2 优化记录

| 时间 | 修改文件 | 修改内容 |
|------|---------|---------|
| v2 | `RecommendServiceImpl.java` | 降级逻辑从2层扩展为4层，区分 `popular`/`new_user`/`personalized` |
| v2 | `RecommendServiceImpl.java` | `buildUserProfile` 改用 `aiStatus=2` 过滤，避免未完成案例污染画像 |
| v2 | `RecommendServiceImpl.java` | 行为记录检查（`interactedCaseIds.isEmpty()`）与特征检查（`isEmpty()`）分离 |
| v2 | `PersonalizedRecommendVO.java` | `basis` 注释新增 `new_user` 说明 |
| v2 | `home.wxml` | 新增 `new_user` 状态的冷启动提示条 |
| v2 | `home.wxss` | 新增冷启动提示条样式（黄色左边线卡片） |
